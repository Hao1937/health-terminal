<!--
 * @Author: wangyuhao wyuhao704@gmail.com
 * @Date: 2026-07-13 20:31:54
 * @LastEditors: wangyuhao wyuhao704@gmail.com
 * @LastEditTime: 2026-07-13 20:35:32
 * @FilePath: \health-terminal\docs\中期任务清单.md
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%AE
-->
# 中期任务清单（TODO）

> 目标：人人环境跑通、首版固件上板、各自认领模块，并把第一个桩替换为真实现。
> 任务按逻辑先后排列，不绑定具体某天，做完一项勾一项；每天结束前 `git commit` 一次，
> 周日为合并日（见 [协作流程](协作流程.md)）。

## 共同前置（三人都做，且需先于各自模块任务）

- [ ] 按 [开发环境搭建.md](开发环境搭建.md) 装齐工具链：Git、CMake、Ninja、arm-none-eabi-gcc、OpenOCD、VSCode（+host 编译器）。
- [ ] `git clone` 克隆仓库，再 `bash scripts/fetch_hal.sh` 获取 HAL。
- [ ] 跑通 host 自检：`cmake --preset host && cmake --build --preset host && ctest --preset host`
      —— `test_filters / test_codec / test_algorithms` 应全部 Passed。
- [ ] **烧录 hello 固件**：`cmake --preset stm32 && cmake --build --preset stm32`，用 OpenOCD 烧入，
      确认 **PC13 LED 闪烁** + USART1(115200) 打印 `health-terminal v0.1` 与自检信息。
- [ ] 通读 [接口文档.md](接口文档.md) 与 [引脚分配表.md](引脚分配表.md)，理解 `hs_status_t`、
      `hs_sensor_t` 接口与 `measurement_record_t`/帧格式（**本周冻结，不得擅改**）。
- [ ] 从 `dev` 切出个人分支：`git checkout dev && git checkout -b feature/<姓名>-<模块>`。

---

## 王宇浩（组长）—— bsp / 滤波 / MAX30102 / 握力 / 评分 / 集成

- [ ] 完成共同前置；确认 `main` 保护、建好 `dev`，将首版可编译工程推上远端。
- [ ] 核对 `bsp/`（i2c/uart/gpio/delay/flash_eeprom）在真板可用：I2C 扫描能探到
      OLED/MPU6050/MAX30102 三个地址（看 `app_init` 打印的 `I2C probe`）；`delay_us` 用逻辑分析仪校准。
- [ ] `algorithms/filters` 已含单元测试；补 1~2 组真实录制数据回归，确认滤波库可交付他人使用。
- [ ] 接 MAX30102（I2C 0x57），将 `modules/max30102.c` 由桩替换为真实现：读 FIFO 得
      RED/IR → 调 `spo2_hr_compute` → 写 `out->primary=心率, secondary=血氧×10`；
      用 `-DMODULE_SET=yuhao` 单编烧板，**实测静息心率并记录**。
- [ ] 接 HX711#2（握力，PA0/PB10），实现 `hx711_grip_measure` 时序 + 标定（kg×10），实测握力。
- [ ] 整理 `health_score` 权重。
- [ ] 汇总三人进度，主持第一次合并日，将稳定点合入 `dev`。

## 查樊听 —— 体重(HX711#1) / 身高(HC-SR04) / 温度(DS18B20)

- [ ] 完成共同前置，烧 hello 固件确认板子正常；切分支 `feature/chafanting-hx711-weight`。
- [ ] 接 HX711#1（体重，PA1/PB11）。先用示波器/逻辑分析仪观察 SCK/DOUT 波形，跑通 24bit 读数。
- [ ] 实现 `modules/hx711_weight.c` 真实现：读原始值 → 减皮重 → 乘标定系数得克数写
      `out->primary`；不稳定时返回 `HS_UNSTABLE`。`-DMODULE_SET=chafanting` 单编烧板。
- [ ] **标定体重**：用标准砝码（5/10/20kg）做两点标定，记录标定系数与误差作为中期数据。
- [ ] 接 HC-SR04（Trig=PB9/Echo=PB8），用 TIM4_CH3 输入捕获测回波换算距离，实现 `hcsr04_measure`。
- [ ] 接 DS18B20（PB5），实现 `ds18b20_measure` 读温度；以温度**补偿声速**修正 HC-SR04 身高。
- [ ] 整理身高/体重/温度单体实测数据；提交并参加合并日。

## 刘晏铭 —— OLED / 键盘 / 反应时间 / 存储 / BLE+Web / 平衡

- [ ] 完成共同前置，烧 hello 固件；切分支 `feature/liuyanming-oled`。
- [ ] 接 SSD1306 OLED（I2C 0x3C），实现 `modules/oled.c` 初始化 + 字符显示；`-DMODULE_SET=liuyanming` 单编烧板。
- [ ] 接 4×4 键盘（行 PB12-15/列 PA4-7），实现 `keypad_scan` 轮询扫描 + 消抖；OLED 回显按键。
- [ ] 反应时间测试：LED=PB1、按键=PB0(EXTI0)，`reaction_measure` 记 t0 → 中断 t1，实测反应时间(ms)，处理抢跑。
- [ ] BLE 联调：`ble_send_record` 已给参考实现（走冻结帧格式）；接 HM-10/JDY-23，用
      **web/dashboard.html** 在安卓 Chrome 上先点"演示"看假数据，再连真机验证 8 项指标解析正确。
- [ ] 接 MPU6050（I2C 0x68），读加速度/陀螺 → `comp_update` 姿态角 → `balance_index` 得晃动指数，实现 `mpu6050_measure`。
- [ ] 实现 `storage`（片内 Flash 历史，基于 `bsp/flash_eeprom` 与 `record_codec` CRC）；提交并参加合并日。

---

## 中期交付判据（合并检查）

- [ ] 三人环境均能 `ctest --preset host` 全绿、能烧 hello 固件看到现象。
- [ ] 各自至少 **1 个模块从桩替换为真实现**并有单体实测数据（为 8.10 中期做准备）。
- [ ] 所有改动不破坏 `MODULE_SET=full` 整机编译；`include/` 契约头未被擅改。
- [ ] `dev` 合出一个稳定点；组长评估是否预打 `v0.5-midterm`。
